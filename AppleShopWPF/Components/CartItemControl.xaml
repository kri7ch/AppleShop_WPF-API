<UserControl x:Class="AppleShopWPF.Components.CartItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="800">
    <Border BorderBrush="#DDD" BorderThickness="1" CornerRadius="16" Padding="16" Margin="12,8" Background="White" HorizontalAlignment="Stretch">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="ProductImage" Grid.Column="0" Width="200" Height="200" Stretch="Uniform" Loaded="ProductImage_Loaded"/>

            <StackPanel Grid.Column="1" Margin="16,0">
                <TextBlock Text="{Binding Product.Name}" FontSize="24" FontWeight="Bold" TextWrapping="Wrap"/>
                <TextBlock Margin="0,6,0,0" Text="{Binding Product.Price, StringFormat=Цена: {0} ₽}" FontSize="20"/>
                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBlock Text="Количество:" VerticalAlignment="Center" FontSize="20"/>
                    <TextBlock Text="{Binding Quantity}" VerticalAlignment="Center" FontSize="20" Margin="6,0,0,0"/>
                    <TextBlock Text="  |  На складе:" VerticalAlignment="Center" FontSize="20"/>
                    <TextBlock Text="{Binding Product.StockQuantity}" VerticalAlignment="Center" FontSize="20" Margin="6,0,0,0"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Style="{StaticResource btn_style}" Content="-" Width="50" Height="50" Click="Decrease_Click" Margin="0,0,6,0" Background="Black" Foreground="White"/>
                <Button Style="{StaticResource btn_style}" Content="+" Width="50" Height="50" Click="Increase_Click" Margin="0,0,6,0" Background="Black" Foreground="White"/>
                <Button Content="Remove" Style="{StaticResource btn_style_buy}" Width="140" Height="50" FontSize="18" Background="#FFB14F4F" Foreground="White" Click="Remove_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>